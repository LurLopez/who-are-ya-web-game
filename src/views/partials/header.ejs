<header class="bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 shadow-sm transition-colors duration-300 w-full z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            
            <div class="flex-shrink-0 flex items-center">
                <a href="/" class="flex items-center gap-2 no-underline">
                    <span class="text-2xl">⚽</span> 
                    <span class="font-bold text-xl tracking-tight text-gray-900 dark:text-white hover:text-green-500 transition-colors">
                        Who Are Ya?
                    </span>
                </a>
            </div>

            <div class="flex items-center gap-4">
                
                <% if (typeof userId !== 'undefined' && userId) { %>
                    
                    <span class="text-sm font-medium text-gray-500 dark:text-gray-300 hidden sm:block">
                        👋 Hola!
                    </span>
                    <a href="/auth/logout" 
                       class="text-sm font-medium text-white bg-red-500 hover:bg-red-600 px-4 py-2 rounded-lg transition-colors">
                        Salir
                    </a>

                <% } else { %>
                    
                    <a href="/auth/login" 
                       class="text-sm font-medium text-gray-700 dark:text-gray-200 hover:text-green-500 transition-colors">
                        Login
                    </a>
                    <a href="/auth/register" 
                       class="text-sm font-medium text-white bg-green-600 hover:bg-green-700 px-4 py-2 rounded-lg transition-colors shadow-md">
                        Registrarse
                    </a>

                <% } %>
            </div>

        </div>
    </div>
</header>